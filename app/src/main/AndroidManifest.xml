<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mat029studio.firstapp.deapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_de"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_de_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Mat029studio.Deapp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Mat029studio.Deapp.DiceActivity" />
    </application>

</manifest>